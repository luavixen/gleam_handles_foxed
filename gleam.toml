name = "handles_foxed"
version = "5.0.0"

description = "Handles is a templating language written in pure Gleam. This fork has been updated for newer versions of Gleam."
licences = ["MIT"]

repository = { type = "github", user = "luavixen", repo = "gleam_handles_foxed" }

gleam = ">= 1.4.0"

internal_modules = [
  "handles/internal",
  "handles/internal/*",
]

[javascript]
typescript_declarations = true

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
